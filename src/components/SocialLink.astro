---
import { Icon } from "astro-icon";

interface Props {
  href: string;
  icon: string;
}

const { href, icon } = Astro.props;
---

<a
  class="transition-all duration-500 hover:scale-105 opacity-70 hover:opacity-100"
  href={href}
>
  <Icon class="w-8 md:w-12 xl:w-14" name={icon} />
</a>
